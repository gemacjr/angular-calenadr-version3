import { TodayIconPipe } from './today-icon.pipe';

describe('TodayIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TodayIconPipe();
    expect(pipe).toBeTruthy();
  });
});
