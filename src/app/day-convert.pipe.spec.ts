import { DayConvertPipe } from './day-convert.pipe';

describe('DayConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new DayConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
