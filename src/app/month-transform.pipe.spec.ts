import { MonthTransformPipe } from './month-transform.pipe';

describe('MonthTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
